// <auto-generated>
//   Generated by codegen/smith.ts from @seamapi/types. Do not edit.
// </auto-generated>
#nullable enable
#pragma warning disable CS0618 // The generated code references its own deprecated members.
using System.Runtime.Serialization;
using System.Text.Json;
using System.Text.Json.Serialization;
using Seam.Http;
using Seam.Models;

namespace Seam.Routes
{
    public sealed class ActionAttempts
    {
        private readonly SeamHttpTransport _transport;
        private readonly ActionAttemptWait _waitForActionAttemptDefault;

        internal ActionAttempts(
            SeamHttpTransport transport,
            ActionAttemptWait waitForActionAttemptDefault
        )
        {
            _transport = transport;
            _waitForActionAttemptDefault = waitForActionAttemptDefault;
        }

        /// <summary>
        /// Request parameters for Get an Action Attempt.
        /// </summary>
        public sealed record GetRequest
        {
            /// <summary>
            /// ID of the action attempt that you want to get.
            /// </summary>
            [JsonPropertyName("action_attempt_id")]
            public required string ActionAttemptId { get; init; }
        }

        public sealed record GetResponse
        {
            /// <summary>
            /// OK
            /// </summary>
            [JsonPropertyName("action_attempt")]
            public ActionAttempt? ActionAttempt { get; init; }
        }

        /// <summary>
        /// Returns a specified [action attempt](https://docs.seam.co/core-concepts/action-attempts).
        /// </summary>
        public async Task<ActionAttempt> GetAsync(
            GetRequest request,
            ActionAttemptWait? waitForActionAttempt = null,
            CancellationToken cancellationToken = default
        )
        {
            var response = await _transport
                .SendAsync<GetResponse>(
                    HttpMethod.Get,
                    "/action_attempts/get",
                    request,
                    cancellationToken
                )
                .ConfigureAwait(false);
            var actionAttempt =
                response.ActionAttempt
                ?? throw new HttpRequestException(
                    "Seam returned no action_attempt for /action_attempts/get"
                );
            return await ActionAttemptResolver
                .ResolveAsync(
                    actionAttempt,
                    _transport,
                    waitForActionAttempt ?? _waitForActionAttemptDefault,
                    cancellationToken
                )
                .ConfigureAwait(false);
        }

        /// <summary>
        /// Request parameters for List Action Attempts.
        /// </summary>
        public sealed record ListRequest
        {
            /// <summary>
            /// IDs of the action attempts that you want to retrieve.
            /// </summary>
            [JsonPropertyName("action_attempt_ids")]
            public List<string>? ActionAttemptIds { get; init; }

            /// <summary>
            /// ID of the device to filter action attempts by.
            /// </summary>
            [JsonPropertyName("device_id")]
            public string? DeviceId { get; init; }

            /// <summary>
            /// Maximum number of records to return per page.
            /// </summary>
            [JsonPropertyName("limit")]
            public int? Limit { get; init; }

            /// <summary>
            /// Identifies the specific page of results to return, obtained from the previous page&apos;s `next_page_cursor`.
            /// </summary>
            [JsonPropertyName("page_cursor")]
            public Optional<string> PageCursor { get; init; }
        }

        public sealed record ListResponse
        {
            /// <summary>
            /// OK
            /// </summary>
            [JsonPropertyName("action_attempts")]
            public List<ActionAttempt>? ActionAttempts { get; init; }

            /// <summary>
            /// The pagination metadata for the page of results.
            /// </summary>
            [JsonPropertyName("pagination")]
            public Pagination? Pagination { get; init; }
        }

        /// <summary>
        /// Returns a list of the [action attempts](https://docs.seam.co/core-concepts/action-attempts) that you specify as an array of `action_attempt_id`s.
        /// </summary>
        public async Task<List<ActionAttempt>> ListAsync(
            ListRequest? request = null,
            CancellationToken cancellationToken = default
        )
        {
            var response = await _transport
                .SendAsync<ListResponse>(
                    HttpMethod.Get,
                    "/action_attempts/list",
                    request,
                    cancellationToken
                )
                .ConfigureAwait(false);
            return response.ActionAttempts
                ?? throw new HttpRequestException(
                    "Seam returned no action_attempts for /action_attempts/list"
                );
        }

        /// <summary>Fetches one page of /action_attempts/list with its pagination metadata.</summary>
        public async Task<SeamPage<ActionAttempt>> ListPageAsync(
            ListRequest? request = null,
            CancellationToken cancellationToken = default
        )
        {
            var response = await _transport
                .SendAsync<ListResponse>(
                    HttpMethod.Get,
                    "/action_attempts/list",
                    request,
                    cancellationToken
                )
                .ConfigureAwait(false);
            var items =
                response.ActionAttempts
                ?? throw new HttpRequestException(
                    "Seam returned no action_attempts for /action_attempts/list"
                );
            var pagination =
                response.Pagination
                ?? throw new HttpRequestException(
                    "Seam returned no pagination for /action_attempts/list"
                );
            return new SeamPage<ActionAttempt>(items, pagination);
        }

        /// <summary>Creates a paginator over /action_attempts/list.</summary>
        public SeamPaginator<ActionAttempt> ListPager(ListRequest? request = null)
        {
            return new SeamPaginator<ActionAttempt>(
                (pageCursor, cancellationToken) =>
                    ListPageAsync(
                        pageCursor == null
                            ? request
                            : (request ?? new ListRequest()) with
                            {
                                PageCursor = pageCursor,
                            },
                        cancellationToken
                    )
            );
        }
    }
}
