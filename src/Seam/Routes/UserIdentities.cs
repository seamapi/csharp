// <auto-generated>
//   Generated by codegen/smith.ts from @seamapi/types. Do not edit.
// </auto-generated>
#nullable enable
#pragma warning disable CS0618 // The generated code references its own deprecated members.
using System.Runtime.Serialization;
using System.Text.Json;
using System.Text.Json.Serialization;
using Seam.Http;
using Seam.Models;

namespace Seam.Routes
{
    public sealed class UserIdentities
    {
        private readonly SeamHttpTransport _transport;
        private readonly ActionAttemptWait _waitForActionAttemptDefault;

        internal UserIdentities(
            SeamHttpTransport transport,
            ActionAttemptWait waitForActionAttemptDefault
        )
        {
            _transport = transport;
            _waitForActionAttemptDefault = waitForActionAttemptDefault;
            Unmanaged = new UserIdentitiesUnmanaged(transport, waitForActionAttemptDefault);
        }

        public UserIdentitiesUnmanaged Unmanaged { get; }

        /// <summary>
        /// Request parameters for Add an ACS User to a User Identity.
        /// </summary>
        public sealed record AddAcsUserRequest
        {
            /// <summary>
            /// ID of the access system user that you want to add to the user identity.
            /// </summary>
            [JsonPropertyName("acs_user_id")]
            public required string AcsUserId { get; init; }

            /// <summary>
            /// ID of the user identity to which you want to add an access system user.
            /// </summary>
            [JsonPropertyName("user_identity_id")]
            public string? UserIdentityId { get; init; }

            /// <summary>
            /// Key of the user identity to which you want to add an access system user.
            /// </summary>
            [JsonPropertyName("user_identity_key")]
            public string? UserIdentityKey { get; init; }
        }

        /// <summary>
        /// Adds a specified [access system user](https://docs.seam.co/low-level-apis/access-systems/user-management) to a specified [user identity](https://docs.seam.co/capability-guides/mobile-access/managing-mobile-app-user-accounts-with-user-identities#what-is-a-user-identity).
        ///
        /// You must specify either `user_identity_id` or `user_identity_key` to identify the user identity.
        ///
        /// If `user_identity_key` is provided, but the user identity doesn&apos;t exist, a new user identity will be created automatically using information from the ACS user.
        /// </summary>
        public async Task AddAcsUserAsync(
            AddAcsUserRequest request,
            CancellationToken cancellationToken = default
        )
        {
            await _transport
                .SendAsync(
                    HttpMethod.Put,
                    "/user_identities/add_acs_user",
                    request,
                    cancellationToken
                )
                .ConfigureAwait(false);
        }

        /// <summary>
        /// Request parameters for Create a User Identity.
        /// </summary>
        public sealed record CreateRequest
        {
            /// <summary>
            /// List of access system IDs to associate with the new user identity through access system users. If there&apos;s no user with the same email address or phone number in the specified access systems, a new access system user is created. If there is an existing user with the same email or phone number in the specified access systems, the user is linked to the user identity.
            /// </summary>
            [JsonPropertyName("acs_system_ids")]
            public List<string>? AcsSystemIds { get; init; }

            /// <summary>
            /// Unique email address for the new user identity.
            /// </summary>
            [JsonPropertyName("email_address")]
            public Optional<string> EmailAddress { get; init; }

            /// <summary>
            /// Full name of the user associated with the new user identity.
            /// </summary>
            [JsonPropertyName("full_name")]
            public Optional<string> FullName { get; init; }

            /// <summary>
            /// Unique phone number for the new user identity in E.164 format (for example, +15555550100).
            /// </summary>
            [JsonPropertyName("phone_number")]
            public Optional<string> PhoneNumber { get; init; }

            /// <summary>
            /// Unique key for the new user identity.
            /// </summary>
            [JsonPropertyName("user_identity_key")]
            public Optional<string> UserIdentityKey { get; init; }
        }

        public sealed record CreateResponse
        {
            /// <summary>
            /// OK
            /// </summary>
            [JsonPropertyName("user_identity")]
            public UserIdentity? UserIdentity { get; init; }
        }

        /// <summary>
        /// Creates a new [user identity](https://docs.seam.co/capability-guides/mobile-access/managing-mobile-app-user-accounts-with-user-identities#what-is-a-user-identity).
        /// </summary>
        public async Task<UserIdentity> CreateAsync(
            CreateRequest? request = null,
            CancellationToken cancellationToken = default
        )
        {
            var response = await _transport
                .SendAsync<CreateResponse>(
                    HttpMethod.Post,
                    "/user_identities/create",
                    request,
                    cancellationToken
                )
                .ConfigureAwait(false);
            return response.UserIdentity
                ?? throw new HttpRequestException(
                    "Seam returned no user_identity for /user_identities/create"
                );
        }

        /// <summary>
        /// Request parameters for Delete a User Identity.
        /// </summary>
        public sealed record DeleteRequest
        {
            /// <summary>
            /// ID of the user identity that you want to delete.
            /// </summary>
            [JsonPropertyName("user_identity_id")]
            public required string UserIdentityId { get; init; }
        }

        /// <summary>
        /// Deletes a specified [user identity](https://docs.seam.co/capability-guides/mobile-access/managing-mobile-app-user-accounts-with-user-identities#what-is-a-user-identity). This deletes the user identity and all associated resources, including any [credentials](https://docs.seam.co/api/acs/credentials), [acs users](https://docs.seam.co/api/acs/users) and [client sessions](https://docs.seam.co/api/client_sessions).
        /// </summary>
        public async Task DeleteAsync(
            DeleteRequest request,
            CancellationToken cancellationToken = default
        )
        {
            await _transport
                .SendAsync(HttpMethod.Delete, "/user_identities/delete", request, cancellationToken)
                .ConfigureAwait(false);
        }

        /// <summary>
        /// Request parameters for Generate an Instant Key.
        /// </summary>
        public sealed record GenerateInstantKeyRequest
        {
            [JsonPropertyName("customization_profile_id")]
            public string? CustomizationProfileId { get; init; }

            /// <summary>
            /// Maximum number of times the instant key can be used. Default: 1.
            /// </summary>
            [JsonPropertyName("max_use_count")]
            public float? MaxUseCount { get; init; }

            /// <summary>
            /// ID of the user identity for which you want to generate an instant key.
            /// </summary>
            [JsonPropertyName("user_identity_id")]
            public required string UserIdentityId { get; init; }
        }

        public sealed record GenerateInstantKeyResponse
        {
            /// <summary>
            /// OK
            /// </summary>
            [JsonPropertyName("instant_key")]
            public InstantKey? InstantKey { get; init; }
        }

        /// <summary>
        /// Generates a new [instant key](https://docs.seam.co/capability-guides/instant-keys) for a specified [user identity](https://docs.seam.co/capability-guides/mobile-access/managing-mobile-app-user-accounts-with-user-identities#what-is-a-user-identity).
        /// </summary>
        public async Task<InstantKey> GenerateInstantKeyAsync(
            GenerateInstantKeyRequest request,
            CancellationToken cancellationToken = default
        )
        {
            var response = await _transport
                .SendAsync<GenerateInstantKeyResponse>(
                    HttpMethod.Post,
                    "/user_identities/generate_instant_key",
                    request,
                    cancellationToken
                )
                .ConfigureAwait(false);
            return response.InstantKey
                ?? throw new HttpRequestException(
                    "Seam returned no instant_key for /user_identities/generate_instant_key"
                );
        }

        /// <summary>
        /// Request parameters for Get a User Identity.
        /// </summary>
        public sealed record GetRequest
        {
            /// <summary>
            /// ID of the user identity that you want to get.
            /// </summary>
            [JsonPropertyName("user_identity_id")]
            public string? UserIdentityId { get; init; }

            [JsonPropertyName("user_identity_key")]
            public string? UserIdentityKey { get; init; }

            internal void Validate()
            {
                if (UserIdentityId == null && UserIdentityKey == null)
                {
                    throw new ArgumentException(
                        "At least one parameter is required for /user_identities/get"
                    );
                }
            }
        }

        public sealed record GetResponse
        {
            /// <summary>
            /// OK
            /// </summary>
            [JsonPropertyName("user_identity")]
            public UserIdentity? UserIdentity { get; init; }
        }

        /// <summary>
        /// Returns a specified [user identity](https://docs.seam.co/capability-guides/mobile-access/managing-mobile-app-user-accounts-with-user-identities#what-is-a-user-identity).
        /// </summary>
        public async Task<UserIdentity> GetAsync(
            GetRequest request,
            CancellationToken cancellationToken = default
        )
        {
            request.Validate();
            var response = await _transport
                .SendAsync<GetResponse>(
                    HttpMethod.Get,
                    "/user_identities/get",
                    request,
                    cancellationToken
                )
                .ConfigureAwait(false);
            return response.UserIdentity
                ?? throw new HttpRequestException(
                    "Seam returned no user_identity for /user_identities/get"
                );
        }

        /// <summary>
        /// Request parameters for Grant a User Identity Access to a Device.
        /// </summary>
        public sealed record GrantAccessToDeviceRequest
        {
            /// <summary>
            /// ID of the managed device to which you want to grant access to the user identity.
            /// </summary>
            [JsonPropertyName("device_id")]
            public required string DeviceId { get; init; }

            /// <summary>
            /// ID of the user identity that you want to grant access to a device.
            /// </summary>
            [JsonPropertyName("user_identity_id")]
            public required string UserIdentityId { get; init; }
        }

        /// <summary>
        /// Grants a specified [user identity](https://docs.seam.co/capability-guides/mobile-access/managing-mobile-app-user-accounts-with-user-identities#what-is-a-user-identity) access to a specified [device](https://docs.seam.co/core-concepts/devices/).
        /// </summary>
        public async Task GrantAccessToDeviceAsync(
            GrantAccessToDeviceRequest request,
            CancellationToken cancellationToken = default
        )
        {
            await _transport
                .SendAsync(
                    HttpMethod.Put,
                    "/user_identities/grant_access_to_device",
                    request,
                    cancellationToken
                )
                .ConfigureAwait(false);
        }

        /// <summary>
        /// Request parameters for List User Identities.
        /// </summary>
        public sealed record ListRequest
        {
            /// <summary>
            /// Timestamp by which to limit returned user identities. Returns user identities created before this timestamp.
            /// </summary>
            [JsonPropertyName("created_before")]
            public string? CreatedBefore { get; init; }

            /// <summary>
            /// `acs_system_id` of the credential manager by which you want to filter the list of user identities.
            /// </summary>
            [JsonPropertyName("credential_manager_acs_system_id")]
            public string? CredentialManagerAcsSystemId { get; init; }

            /// <summary>
            /// Maximum number of records to return per page.
            /// </summary>
            [JsonPropertyName("limit")]
            public int? Limit { get; init; }

            /// <summary>
            /// Identifies the specific page of results to return, obtained from the previous page&apos;s `next_page_cursor`.
            /// </summary>
            [JsonPropertyName("page_cursor")]
            public Optional<string> PageCursor { get; init; }

            /// <summary>
            /// String for which to search. Filters returned user identities to include all records that satisfy a partial match using `full_name`, `phone_number`, `email_address` or `user_identity_id`.
            /// </summary>
            [JsonPropertyName("search")]
            public string? Search { get; init; }

            /// <summary>
            /// Array of user identity IDs by which to filter the list of user identities.
            /// </summary>
            [JsonPropertyName("user_identity_ids")]
            public List<string>? UserIdentityIds { get; init; }
        }

        public sealed record ListResponse
        {
            /// <summary>
            /// OK
            /// </summary>
            [JsonPropertyName("user_identities")]
            public List<UserIdentity>? UserIdentities { get; init; }

            /// <summary>
            /// The pagination metadata for the page of results.
            /// </summary>
            [JsonPropertyName("pagination")]
            public Pagination? Pagination { get; init; }
        }

        /// <summary>
        /// Returns a list of all [user identities](https://docs.seam.co/capability-guides/mobile-access/managing-mobile-app-user-accounts-with-user-identities#what-is-a-user-identity).
        /// </summary>
        public async Task<List<UserIdentity>> ListAsync(
            ListRequest? request = null,
            CancellationToken cancellationToken = default
        )
        {
            var response = await _transport
                .SendAsync<ListResponse>(
                    HttpMethod.Get,
                    "/user_identities/list",
                    request,
                    cancellationToken
                )
                .ConfigureAwait(false);
            return response.UserIdentities
                ?? throw new HttpRequestException(
                    "Seam returned no user_identities for /user_identities/list"
                );
        }

        /// <summary>Fetches one page of /user_identities/list with its pagination metadata.</summary>
        public async Task<SeamPage<UserIdentity>> ListPageAsync(
            ListRequest? request = null,
            CancellationToken cancellationToken = default
        )
        {
            var response = await _transport
                .SendAsync<ListResponse>(
                    HttpMethod.Get,
                    "/user_identities/list",
                    request,
                    cancellationToken
                )
                .ConfigureAwait(false);
            var items =
                response.UserIdentities
                ?? throw new HttpRequestException(
                    "Seam returned no user_identities for /user_identities/list"
                );
            var pagination =
                response.Pagination
                ?? throw new HttpRequestException(
                    "Seam returned no pagination for /user_identities/list"
                );
            return new SeamPage<UserIdentity>(items, pagination);
        }

        /// <summary>Creates a paginator over /user_identities/list.</summary>
        public SeamPaginator<UserIdentity> ListPager(ListRequest? request = null)
        {
            return new SeamPaginator<UserIdentity>(
                (pageCursor, cancellationToken) =>
                    ListPageAsync(
                        pageCursor == null
                            ? request
                            : (request ?? new ListRequest()) with
                            {
                                PageCursor = pageCursor,
                            },
                        cancellationToken
                    )
            );
        }

        /// <summary>
        /// Request parameters for List Accessible Devices for a User Identity.
        /// </summary>
        public sealed record ListAccessibleDevicesRequest
        {
            /// <summary>
            /// ID of the user identity for which you want to retrieve all accessible devices.
            /// </summary>
            [JsonPropertyName("user_identity_id")]
            public required string UserIdentityId { get; init; }
        }

        public sealed record ListAccessibleDevicesResponse
        {
            /// <summary>
            /// OK
            /// </summary>
            [JsonPropertyName("devices")]
            public List<Device>? Devices { get; init; }
        }

        /// <summary>
        /// Returns a list of all [devices](https://docs.seam.co/core-concepts/devices) associated with a specified [user identity](https://docs.seam.co/capability-guides/mobile-access/managing-mobile-app-user-accounts-with-user-identities#what-is-a-user-identity). This includes devices derived from the access grants assigned to the user identity and devices directly linked to the user identity.
        /// </summary>
        public async Task<List<Device>> ListAccessibleDevicesAsync(
            ListAccessibleDevicesRequest request,
            CancellationToken cancellationToken = default
        )
        {
            var response = await _transport
                .SendAsync<ListAccessibleDevicesResponse>(
                    HttpMethod.Get,
                    "/user_identities/list_accessible_devices",
                    request,
                    cancellationToken
                )
                .ConfigureAwait(false);
            return response.Devices
                ?? throw new HttpRequestException(
                    "Seam returned no devices for /user_identities/list_accessible_devices"
                );
        }

        /// <summary>
        /// Request parameters for List Accessible Entrances for a User Identity.
        /// </summary>
        public sealed record ListAccessibleEntrancesRequest
        {
            /// <summary>
            /// ID of the user identity for which you want to retrieve all accessible entrances.
            /// </summary>
            [JsonPropertyName("user_identity_id")]
            public required string UserIdentityId { get; init; }
        }

        public sealed record ListAccessibleEntrancesResponse
        {
            /// <summary>
            /// OK
            /// </summary>
            [JsonPropertyName("acs_entrances")]
            public List<AcsEntrance>? AcsEntrances { get; init; }
        }

        /// <summary>
        /// Returns a list of all [ACS entrances](https://docs.seam.co/api/acs/entrances) accessible to a specified [user identity](https://docs.seam.co/capability-guides/mobile-access/managing-mobile-app-user-accounts-with-user-identities#what-is-a-user-identity). This includes entrances derived from the access grants assigned to the user identity and entrances accessible through ACS users linked to the user identity.
        /// </summary>
        public async Task<List<AcsEntrance>> ListAccessibleEntrancesAsync(
            ListAccessibleEntrancesRequest request,
            CancellationToken cancellationToken = default
        )
        {
            var response = await _transport
                .SendAsync<ListAccessibleEntrancesResponse>(
                    HttpMethod.Get,
                    "/user_identities/list_accessible_entrances",
                    request,
                    cancellationToken
                )
                .ConfigureAwait(false);
            return response.AcsEntrances
                ?? throw new HttpRequestException(
                    "Seam returned no acs_entrances for /user_identities/list_accessible_entrances"
                );
        }

        /// <summary>
        /// Request parameters for List ACS Systems Associated with a User Identity.
        /// </summary>
        public sealed record ListAcsSystemsRequest
        {
            /// <summary>
            /// ID of the user identity for which you want to retrieve all access systems.
            /// </summary>
            [JsonPropertyName("user_identity_id")]
            public required string UserIdentityId { get; init; }
        }

        public sealed record ListAcsSystemsResponse
        {
            /// <summary>
            /// OK
            /// </summary>
            [JsonPropertyName("acs_systems")]
            public List<AcsSystem>? AcsSystems { get; init; }
        }

        /// <summary>
        /// Returns a list of all [access systems](https://docs.seam.co/low-level-apis/access-systems) associated with a specified [user identity](https://docs.seam.co/capability-guides/mobile-access/managing-mobile-app-user-accounts-with-user-identities#what-is-a-user-identity).
        /// </summary>
        public async Task<List<AcsSystem>> ListAcsSystemsAsync(
            ListAcsSystemsRequest request,
            CancellationToken cancellationToken = default
        )
        {
            var response = await _transport
                .SendAsync<ListAcsSystemsResponse>(
                    HttpMethod.Get,
                    "/user_identities/list_acs_systems",
                    request,
                    cancellationToken
                )
                .ConfigureAwait(false);
            return response.AcsSystems
                ?? throw new HttpRequestException(
                    "Seam returned no acs_systems for /user_identities/list_acs_systems"
                );
        }

        /// <summary>
        /// Request parameters for List ACS Users Associated with a User Identity.
        /// </summary>
        public sealed record ListAcsUsersRequest
        {
            /// <summary>
            /// ID of the user identity for which you want to retrieve all access system users.
            /// </summary>
            [JsonPropertyName("user_identity_id")]
            public required string UserIdentityId { get; init; }
        }

        public sealed record ListAcsUsersResponse
        {
            /// <summary>
            /// OK
            /// </summary>
            [JsonPropertyName("acs_users")]
            public List<AcsUser>? AcsUsers { get; init; }
        }

        /// <summary>
        /// Returns a list of all [access system users](https://docs.seam.co/low-level-apis/access-systems/user-management) assigned to a specified [user identity](https://docs.seam.co/capability-guides/mobile-access/managing-mobile-app-user-accounts-with-user-identities#what-is-a-user-identity).
        /// </summary>
        public async Task<List<AcsUser>> ListAcsUsersAsync(
            ListAcsUsersRequest request,
            CancellationToken cancellationToken = default
        )
        {
            var response = await _transport
                .SendAsync<ListAcsUsersResponse>(
                    HttpMethod.Get,
                    "/user_identities/list_acs_users",
                    request,
                    cancellationToken
                )
                .ConfigureAwait(false);
            return response.AcsUsers
                ?? throw new HttpRequestException(
                    "Seam returned no acs_users for /user_identities/list_acs_users"
                );
        }

        /// <summary>
        /// Request parameters for Merge User Identities.
        /// </summary>
        public sealed record MergeRequest
        {
            /// <summary>
            /// IDs of the user identities to merge into the primary user identity. These user identities are deleted.
            /// </summary>
            [JsonPropertyName("merged_user_identity_ids")]
            public List<string>? MergedUserIdentityIds { get; init; }

            /// <summary>
            /// ID of the primary user identity to keep.
            /// </summary>
            [JsonPropertyName("user_identity_id")]
            public string? UserIdentityId { get; init; }

            /// <summary>
            /// Keys of the user identities to merge into the primary user identity. These user identities are deleted.
            /// </summary>
            [JsonPropertyName("merged_user_identity_keys")]
            public List<string>? MergedUserIdentityKeys { get; init; }

            /// <summary>
            /// Key of the primary user identity to keep.
            /// </summary>
            [JsonPropertyName("user_identity_key")]
            public string? UserIdentityKey { get; init; }

            internal void Validate()
            {
                if (
                    MergedUserIdentityIds == null
                    && UserIdentityId == null
                    && MergedUserIdentityKeys == null
                    && UserIdentityKey == null
                )
                {
                    throw new ArgumentException(
                        "At least one parameter is required for /user_identities/merge"
                    );
                }
            }
        }

        /// <summary>
        /// Merges one or more [user identities](https://docs.seam.co/capability-guides/mobile-access/managing-mobile-app-user-accounts-with-user-identities#what-is-a-user-identity) into a primary user identity, for when the same person ended up with more than one user identity.
        ///
        /// The primary user identity takes on any email address or phone number it was missing from the user identities merged into it, and the merged user identities are then deleted. Their IDs and keys keep working: looking one up returns the primary user identity, and they are listed on it as `merged_user_identity_ids` and `merged_user_identity_keys`.
        ///
        /// Access grants, access system users, client sessions and other resources belonging to the merged user identities are moved to the primary user identity.
        ///
        /// Identify the user identities either by ID or by key, but not both in the same request. Repeating a merge that has already been applied makes no further changes.
        /// </summary>
        public async Task MergeAsync(
            MergeRequest request,
            CancellationToken cancellationToken = default
        )
        {
            request.Validate();
            await _transport
                .SendAsync(HttpMethod.Post, "/user_identities/merge", request, cancellationToken)
                .ConfigureAwait(false);
        }

        /// <summary>
        /// Request parameters for Remove an ACS User from a User Identity.
        /// </summary>
        public sealed record RemoveAcsUserRequest
        {
            /// <summary>
            /// ID of the access system user that you want to remove from the user identity..
            /// </summary>
            [JsonPropertyName("acs_user_id")]
            public required string AcsUserId { get; init; }

            /// <summary>
            /// ID of the user identity from which you want to remove an access system user.
            /// </summary>
            [JsonPropertyName("user_identity_id")]
            public required string UserIdentityId { get; init; }
        }

        /// <summary>
        /// Removes a specified [access system user](https://docs.seam.co/low-level-apis/access-systems/user-management) from a specified [user identity](https://docs.seam.co/capability-guides/mobile-access/managing-mobile-app-user-accounts-with-user-identities#what-is-a-user-identity).
        /// </summary>
        public async Task RemoveAcsUserAsync(
            RemoveAcsUserRequest request,
            CancellationToken cancellationToken = default
        )
        {
            await _transport
                .SendAsync(
                    HttpMethod.Delete,
                    "/user_identities/remove_acs_user",
                    request,
                    cancellationToken
                )
                .ConfigureAwait(false);
        }

        /// <summary>
        /// Request parameters for Revoke Access to a Device from a User Identity.
        /// </summary>
        public sealed record RevokeAccessToDeviceRequest
        {
            /// <summary>
            /// ID of the managed device to which you want to revoke access from the user identity.
            /// </summary>
            [JsonPropertyName("device_id")]
            public required string DeviceId { get; init; }

            /// <summary>
            /// ID of the user identity from which you want to revoke access to a device.
            /// </summary>
            [JsonPropertyName("user_identity_id")]
            public required string UserIdentityId { get; init; }
        }

        /// <summary>
        /// Revokes access to a specified [device](https://docs.seam.co/core-concepts/devices/) from a specified [user identity](https://docs.seam.co/capability-guides/mobile-access/managing-mobile-app-user-accounts-with-user-identities#what-is-a-user-identity).
        /// </summary>
        public async Task RevokeAccessToDeviceAsync(
            RevokeAccessToDeviceRequest request,
            CancellationToken cancellationToken = default
        )
        {
            await _transport
                .SendAsync(
                    HttpMethod.Delete,
                    "/user_identities/revoke_access_to_device",
                    request,
                    cancellationToken
                )
                .ConfigureAwait(false);
        }

        /// <summary>
        /// Request parameters for Update a User Identity.
        /// </summary>
        public sealed record UpdateRequest
        {
            /// <summary>
            /// Unique email address for the user identity.
            /// </summary>
            [JsonPropertyName("email_address")]
            public Optional<string> EmailAddress { get; init; }

            /// <summary>
            /// Full name of the user associated with the user identity.
            /// </summary>
            [JsonPropertyName("full_name")]
            public Optional<string> FullName { get; init; }

            /// <summary>
            /// Unique phone number for the user identity.
            /// </summary>
            [JsonPropertyName("phone_number")]
            public Optional<string> PhoneNumber { get; init; }

            /// <summary>
            /// ID of the user identity that you want to update.
            /// </summary>
            [JsonPropertyName("user_identity_id")]
            public required string UserIdentityId { get; init; }

            /// <summary>
            /// Unique key for the user identity.
            /// </summary>
            [JsonPropertyName("user_identity_key")]
            public Optional<string> UserIdentityKey { get; init; }
        }

        /// <summary>
        /// Updates a specified [user identity](https://docs.seam.co/capability-guides/mobile-access/managing-mobile-app-user-accounts-with-user-identities#what-is-a-user-identity).
        /// </summary>
        public async Task UpdateAsync(
            UpdateRequest request,
            CancellationToken cancellationToken = default
        )
        {
            await _transport
                .SendAsync(HttpMethod.Patch, "/user_identities/update", request, cancellationToken)
                .ConfigureAwait(false);
        }
    }
}
