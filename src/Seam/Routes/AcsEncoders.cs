// <auto-generated>
//   Generated by codegen/smith.ts from @seamapi/types. Do not edit.
// </auto-generated>
#nullable enable
#pragma warning disable CS0618 // The generated code references its own deprecated members.
using System.Runtime.Serialization;
using System.Text.Json;
using System.Text.Json.Serialization;
using Seam.Http;
using Seam.Models;

namespace Seam.Routes
{
    public sealed class AcsEncoders
    {
        private readonly SeamHttpTransport _transport;
        private readonly ActionAttemptWait _waitForActionAttemptDefault;

        internal AcsEncoders(
            SeamHttpTransport transport,
            ActionAttemptWait waitForActionAttemptDefault
        )
        {
            _transport = transport;
            _waitForActionAttemptDefault = waitForActionAttemptDefault;
            Simulate = new AcsEncodersSimulate(transport, waitForActionAttemptDefault);
        }

        public AcsEncodersSimulate Simulate { get; }

        /// <summary>
        /// Request parameters for Encode a Credential.
        /// </summary>
        public sealed record EncodeCredentialRequest
        {
            /// <summary>
            /// ID of the `access_method` to encode onto a card.
            /// </summary>
            [JsonPropertyName("access_method_id")]
            public string? AccessMethodId { get; init; }

            /// <summary>
            /// ID of the `acs_credential` to encode onto a card.
            /// </summary>
            [JsonPropertyName("acs_credential_id")]
            public string? AcsCredentialId { get; init; }

            /// <summary>
            /// ID of the `acs_encoder` to use to encode the `acs_credential`.
            /// </summary>
            [JsonPropertyName("acs_encoder_id")]
            public required string AcsEncoderId { get; init; }
        }

        public sealed record EncodeCredentialResponse
        {
            /// <summary>
            /// OK
            /// </summary>
            [JsonPropertyName("action_attempt")]
            public ActionAttempt? ActionAttempt { get; init; }
        }

        /// <summary>
        /// Encodes an existing [credential](https://docs.seam.co/low-level-apis/access-systems/managing-credentials) onto a plastic card placed on the specified [encoder](https://docs.seam.co/low-level-apis/access-systems/working-with-card-encoders-and-scanners). Either provide an `acs_credential_id` or an `access_method_id`
        /// </summary>
        public async Task<ActionAttempt> EncodeCredentialAsync(
            EncodeCredentialRequest request,
            ActionAttemptWait? waitForActionAttempt = null,
            CancellationToken cancellationToken = default
        )
        {
            var response = await _transport
                .SendAsync<EncodeCredentialResponse>(
                    HttpMethod.Post,
                    "/acs/encoders/encode_credential",
                    request,
                    "action_attempt",
                    cancellationToken
                )
                .ConfigureAwait(false);
            var actionAttempt = response.Read(r => r.ActionAttempt);
            return await ActionAttemptResolver
                .ResolveAsync(
                    actionAttempt,
                    _transport,
                    waitForActionAttempt ?? _waitForActionAttemptDefault,
                    cancellationToken
                )
                .ConfigureAwait(false);
        }

        /// <summary>
        /// Request parameters for Get an Encoder.
        /// </summary>
        public sealed record GetRequest
        {
            /// <summary>
            /// ID of the encoder that you want to get.
            /// </summary>
            [JsonPropertyName("acs_encoder_id")]
            public required string AcsEncoderId { get; init; }
        }

        public sealed record GetResponse
        {
            /// <summary>
            /// OK
            /// </summary>
            [JsonPropertyName("acs_encoder")]
            public AcsEncoder? AcsEncoder { get; init; }
        }

        /// <summary>
        /// Returns a specified [encoder](https://docs.seam.co/low-level-apis/access-systems/working-with-card-encoders-and-scanners).
        /// </summary>
        public async Task<AcsEncoder> GetAsync(
            GetRequest request,
            CancellationToken cancellationToken = default
        )
        {
            var response = await _transport
                .SendAsync<GetResponse>(
                    HttpMethod.Get,
                    "/acs/encoders/get",
                    request,
                    "acs_encoder",
                    cancellationToken
                )
                .ConfigureAwait(false);
            return response.Read(r => r.AcsEncoder);
        }

        /// <summary>
        /// Request parameters for List Encoders.
        /// </summary>
        public sealed record ListRequest
        {
            /// <summary>
            /// IDs of the encoders that you want to retrieve.
            /// </summary>
            [JsonPropertyName("acs_encoder_ids")]
            public List<string>? AcsEncoderIds { get; init; }

            /// <summary>
            /// ID of the access system for which you want to retrieve all encoders.
            /// </summary>
            [JsonPropertyName("acs_system_id")]
            public string? AcsSystemId { get; init; }

            /// <summary>
            /// IDs of the access systems for which you want to retrieve all encoders.
            /// </summary>
            [JsonPropertyName("acs_system_ids")]
            public List<string>? AcsSystemIds { get; init; }

            /// <summary>
            /// Number of encoders to return.
            /// </summary>
            [JsonPropertyName("limit")]
            public float? Limit { get; init; }

            /// <summary>
            /// Identifies the specific page of results to return, obtained from the previous page&apos;s `next_page_cursor`.
            /// </summary>
            [JsonPropertyName("page_cursor")]
            public Optional<string> PageCursor { get; init; }
        }

        public sealed record ListResponse
        {
            /// <summary>
            /// OK
            /// </summary>
            [JsonPropertyName("acs_encoders")]
            public List<AcsEncoder>? AcsEncoders { get; init; }

            /// <summary>
            /// The pagination metadata for the page of results.
            /// </summary>
            [JsonPropertyName("pagination")]
            public Pagination? Pagination { get; init; }
        }

        /// <summary>
        /// Returns a list of all [encoders](https://docs.seam.co/low-level-apis/access-systems/working-with-card-encoders-and-scanners).
        /// </summary>
        public async Task<List<AcsEncoder>> ListAsync(
            ListRequest? request = null,
            CancellationToken cancellationToken = default
        )
        {
            var response = await _transport
                .SendAsync<ListResponse>(
                    HttpMethod.Get,
                    "/acs/encoders/list",
                    request,
                    "acs_encoders",
                    cancellationToken
                )
                .ConfigureAwait(false);
            return response.Read(r => r.AcsEncoders);
        }

        /// <summary>Fetches one page of /acs/encoders/list with its pagination metadata.</summary>
        public async Task<SeamPage<AcsEncoder>> ListPageAsync(
            ListRequest? request = null,
            CancellationToken cancellationToken = default
        )
        {
            var response = await _transport
                .SendAsync<ListResponse>(
                    HttpMethod.Get,
                    "/acs/encoders/list",
                    request,
                    "acs_encoders",
                    cancellationToken
                )
                .ConfigureAwait(false);
            return new SeamPage<AcsEncoder>(
                response.Read(r => r.AcsEncoders),
                response.Read("pagination", r => r.Pagination)
            );
        }

        /// <summary>Creates a paginator over /acs/encoders/list.</summary>
        public SeamPaginator<AcsEncoder> ListPager(ListRequest? request = null)
        {
            return new SeamPaginator<AcsEncoder>(
                (pageCursor, cancellationToken) =>
                    ListPageAsync(
                        pageCursor == null
                            ? request
                            : (request ?? new ListRequest()) with
                            {
                                PageCursor = pageCursor,
                            },
                        cancellationToken
                    )
            );
        }

        /// <summary>
        /// Request parameters for Scan a Credential.
        /// </summary>
        public sealed record ScanCredentialRequest
        {
            /// <summary>
            /// ID of the encoder to use for the scan.
            /// </summary>
            [JsonPropertyName("acs_encoder_id")]
            public required string AcsEncoderId { get; init; }

            /// <summary>
            /// Salto KS-specific metadata for the scan action.
            /// </summary>
            [JsonPropertyName("salto_ks_metadata")]
            public ScanCredentialRequestSaltoKsMetadata? SaltoKsMetadata { get; init; }
        }

        public sealed record ScanCredentialRequestSaltoKsMetadata
        {
            /// <summary>
            /// When true, activates tag registration mode on the encoder to detect new, unregistered tags. When false, only detects existing tags already registered in the system. Defaults to false.
            /// </summary>
            [JsonPropertyName("detect_new_tags")]
            public bool? DetectNewTags { get; init; }
        }

        public sealed record ScanCredentialResponse
        {
            /// <summary>
            /// OK
            /// </summary>
            [JsonPropertyName("action_attempt")]
            public ActionAttempt? ActionAttempt { get; init; }
        }

        /// <summary>
        /// Scans an encoded [acs_credential](https://docs.seam.co/low-level-apis/access-systems/managing-credentials) from a plastic card placed on the specified [encoder](https://docs.seam.co/low-level-apis/access-systems/working-with-card-encoders-and-scanners).
        /// </summary>
        public async Task<ActionAttempt> ScanCredentialAsync(
            ScanCredentialRequest request,
            ActionAttemptWait? waitForActionAttempt = null,
            CancellationToken cancellationToken = default
        )
        {
            var response = await _transport
                .SendAsync<ScanCredentialResponse>(
                    HttpMethod.Post,
                    "/acs/encoders/scan_credential",
                    request,
                    "action_attempt",
                    cancellationToken
                )
                .ConfigureAwait(false);
            var actionAttempt = response.Read(r => r.ActionAttempt);
            return await ActionAttemptResolver
                .ResolveAsync(
                    actionAttempt,
                    _transport,
                    waitForActionAttempt ?? _waitForActionAttemptDefault,
                    cancellationToken
                )
                .ConfigureAwait(false);
        }

        /// <summary>
        /// Request parameters for Scan to Assign a Credential.
        /// </summary>
        public sealed record ScanToAssignCredentialRequest
        {
            /// <summary>
            /// ID of the `acs_encoder` to use to scan the credential.
            /// </summary>
            [JsonPropertyName("acs_encoder_id")]
            public required string AcsEncoderId { get; init; }

            /// <summary>
            /// ID of the `acs_user` to assign the scanned credential to.
            /// </summary>
            [JsonPropertyName("acs_user_id")]
            public string? AcsUserId { get; init; }

            /// <summary>
            /// Salto KS-specific metadata for the scan action.
            /// </summary>
            [JsonPropertyName("salto_ks_metadata")]
            public ScanToAssignCredentialRequestSaltoKsMetadata? SaltoKsMetadata { get; init; }

            /// <summary>
            /// ID of the `user_identity` to assign the scanned credential to. If the ACS system contains an ACS user linked to this user identity, it is used. Otherwise, one is created.
            /// </summary>
            [JsonPropertyName("user_identity_id")]
            public string? UserIdentityId { get; init; }
        }

        public sealed record ScanToAssignCredentialRequestSaltoKsMetadata
        {
            /// <summary>
            /// When true, activates tag registration mode on the encoder to detect new, unregistered tags. When false, only detects existing tags already registered in the system. Defaults to false.
            /// </summary>
            [JsonPropertyName("detect_new_tags")]
            public bool? DetectNewTags { get; init; }
        }

        public sealed record ScanToAssignCredentialResponse
        {
            /// <summary>
            /// OK
            /// </summary>
            [JsonPropertyName("action_attempt")]
            public ActionAttempt? ActionAttempt { get; init; }
        }

        /// <summary>
        /// Scans a physical card placed on the specified [encoder](https://docs.seam.co/low-level-apis/access-systems/working-with-card-encoders-and-scanners) and assigns the scanned credential to an ACS user. Provide either an `acs_user_id` or a `user_identity_id`.
        /// </summary>
        public async Task<ActionAttempt> ScanToAssignCredentialAsync(
            ScanToAssignCredentialRequest request,
            ActionAttemptWait? waitForActionAttempt = null,
            CancellationToken cancellationToken = default
        )
        {
            var response = await _transport
                .SendAsync<ScanToAssignCredentialResponse>(
                    HttpMethod.Post,
                    "/acs/encoders/scan_to_assign_credential",
                    request,
                    "action_attempt",
                    cancellationToken
                )
                .ConfigureAwait(false);
            var actionAttempt = response.Read(r => r.ActionAttempt);
            return await ActionAttemptResolver
                .ResolveAsync(
                    actionAttempt,
                    _transport,
                    waitForActionAttempt ?? _waitForActionAttemptDefault,
                    cancellationToken
                )
                .ConfigureAwait(false);
        }
    }
}
